name: Daily Gmail Digest

on:
  schedule:
    - cron: '0 6 * * *'   # 06:00 UTC = 09:00 Israel time
  workflow_dispatch:        # also lets you trigger it manually from GitHub

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:

      # ── 1. Get the code ────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Python ──────────────────────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      # ── 3. Write sensitive files from GitHub Secrets ───────────────────────
      - name: Write token.json from secret
        run: echo '${{ secrets.GMAIL_TOKEN_JSON }}' > token.json

      - name: Create output directory
        run: mkdir -p output

      # ── 4. Sync persistent state from PythonAnywhere ──────────────────────
      #    (user may have toggled domains via the Settings page; the domain
      #    history must accumulate across runs)
      - name: Download ignored_domains from PythonAnywhere
        run: |
          HTTP_STATUS=$(curl -s -o ignored_domains.json -w "%{http_code}" \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/ignored_domains.json")
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "No ignored_domains.json on server yet — using empty list"
            echo '[]' > ignored_domains.json
          fi

      - name: Download all_domains history from PythonAnywhere
        run: |
          HTTP_STATUS=$(curl -s -o all_domains.json -w "%{http_code}" \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/all_domains.json")
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "No all_domains.json on server yet — starting fresh"
            echo '[]' > all_domains.json
          fi

      - name: Download last_archived_at from PythonAnywhere
        run: |
          HTTP_STATUS=$(curl -s -o last_archived_at.json -w "%{http_code}" \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/last_archived_at.json")
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "No last_archived_at.json yet — will fall back to 24h window"
            echo '{}' > last_archived_at.json
          fi

      # ── 5. Run the digest ──────────────────────────────────────────────────
      - name: Run digest.py
        env:
          ANTHROPIC_API_KEY:  ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          DIGEST_URL:         https://nattya01.pythonanywhere.com
          DIGEST_AUTH_TOKEN:  ${{ secrets.DIGEST_AUTH_TOKEN }}
        run: python digest.py

      # ── 6. Upload outputs to PythonAnywhere ───────────────────────────────
      #    Flask app on PythonAnywhere serves these files
      - name: Upload digest.html
        if: always()
        run: |
          [ -f output/digest.html ] && curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@output/digest.html" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/output/digest.html" || true

      - name: Upload digest_meta.json
        if: always()
        run: |
          [ -f output/digest_meta.json ] && curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@output/digest_meta.json" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/output/digest_meta.json" || true

      - name: Upload run_log.json
        if: always()
        run: |
          [ -f output/run_log.json ] && curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@output/run_log.json" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/output/run_log.json" || true

      - name: Upload refreshed token.json
        if: always()
        run: |
          curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@token.json" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/token.json"

      - name: Upload all_domains history
        if: always()
        run: |
          [ -f all_domains.json ] && curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@all_domains.json" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/all_domains.json" || true

      - name: Upload flask_app.py (code refresh)
        if: always()
        run: |
          curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            -F "content=@flask_app.py" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/files/path/home/nattya01/gmail_digest/flask_app.py"

      - name: Reload PythonAnywhere webapp
        if: always()
        run: |
          curl -s -X POST \
            -H "Authorization: Token ${{ secrets.PA_API_TOKEN }}" \
            "https://www.pythonanywhere.com/api/v0/user/nattya01/webapps/nattya01.pythonanywhere.com/reload/" || true
